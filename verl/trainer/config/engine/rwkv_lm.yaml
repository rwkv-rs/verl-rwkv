# Target class for this configuration
_target_: verl.workers.config.RWKVLMEngineConfig

# Native rwkv-lm repository path
rwkv_lm_path: ???

# Native training precision
precision: bf16

# Context length passed to rwkv-lm
ctx_len: null

# RWKV head size passed to rwkv-lm
head_size: null

# Native RWKV gradient checkpointing. Defaults to enabled when infctx is enabled.
grad_cp: null

# Enable native RWKV infinite-context training by splitting ctx_len into chunks.
infctx: false

# RWKV sequence chunk length used when infctx is enabled. Must be divisible by 16.
chunk_ctx: null

# Native rwkv-lm environment variables
native_env: {}

# Whether to offload model parameters to CPU
param_offload: false

# Whether to offload optimizer state to CPU
optimizer_offload: false

# Whether the engine is forward only
forward_only: false

# Strategy name
strategy: rwkv_lm

# Mixed precision training param dtype
dtype: bfloat16
